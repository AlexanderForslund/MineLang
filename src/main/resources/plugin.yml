main: dev.forslund.mc.MineLang
name: MineLang
version: 0.9
description: MineLang interpreter.
api-version: 1.19
prefix: MineLang
authors: [ Duckkyy ]
website: forslund.dev
commands:
  interpreter:
    aliases: [ i ]
    usage: /<command> [run/reset]
    description: Interpreter commands.
  debug:
    aliases: [d]
    usage: /debug [set] <registry index>
    description: Debug commands for interpreter and registries.